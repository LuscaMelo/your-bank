export interface ProductCard {
    category: string
    icon: string
    title: string
    text: string
    border: boolean
}

export interface TestimonialsCard {
    category: string
    testimony: string
    name: string
}

